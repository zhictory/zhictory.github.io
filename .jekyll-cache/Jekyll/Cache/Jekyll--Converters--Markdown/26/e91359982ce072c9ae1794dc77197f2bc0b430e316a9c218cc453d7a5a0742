I"�<p><a href="https://docs.npmjs.com/packages-and-modules/contributing-packages-to-the-registry">官网教程</a></p>

<p>新建一个项目，完成插件代码，然后在目录内并执行命令：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// 生成 package.json
npm init

// 在 https://www.npmjs.com 注册账号，并使用该账号 login
npm login

// 发布到 npm 社区
npm publish
</code></pre></div></div>

<p>注意：</p>
<ol>
  <li>login 之后可以使用 <code class="language-plaintext highlighter-rouge">npm whoami</code> 检测当前用户是哪个账号。</li>
  <li><code class="language-plaintext highlighter-rouge">npm publish</code> 时可能会遇到如下问题而无法 publish：
    <ol>
      <li>package name 跟社区里现有的 package 重复了</li>
      <li>package 有版本控制但代码修改后未提交，使用 <code class="language-plaintext highlighter-rouge">git commit</code> 解决。</li>
      <li>package 有修改但未更新 version，使用 <code class="language-plaintext highlighter-rouge">npm version x.y.z</code> 解决。</li>
      <li>npm 未指定官方 <a href="https://registry.npmjs.org">registry manager</a>，使用 <code class="language-plaintext highlighter-rouge">nrm use npm</code> 来指定。</li>
    </ol>
  </li>
  <li>不建议使用 <code class="language-plaintext highlighter-rouge">npm unpublish --force</code> 来撤销发布，建议使用 <code class="language-plaintext highlighter-rouge">npm deprecate</code> 来表示不再维护。</li>
</ol>

<p>实践：</p>
<ol>
  <li>一般人把 npm package 当成模块来 import 或 require 的，所以插件的代码可以用 export 或 module.exports 导出且无需构建。</li>
  <li>package 的目录一般含有：js 入口文件、package.json、README.md。</li>
</ol>

<p>参考：</p>
<ol>
  <li><a href="https://www.youtube.com/watch?v=rTsz09zRuTU">How to create and publish NPM Packages?</a></li>
  <li><a href="https://cloud.tencent.com/developer/article/1011975">利用npm安装/删除/发布/更新/撤销发布包</a></li>
  <li><a href="https://lellansin.wordpress.com/2014/05/09/npm-publish-%E5%8F%91%E5%B8%83%E7%A4%BA%E4%BE%8B/">npm publish 发布示例</a></li>
</ol>

:ET